import {useEffect,useRef,type ReactNode} from 'react';
import {X,Zap,Flame,Orbit,Sparkles} from 'lucide-react';
export function Modal({title,children,onClose}:{title:string;children:ReactNode;onClose:()=>void}){const ref=useRef<HTMLDialogElement>(null);useEffect(()=>{const dialog=ref.current;dialog?.showModal();return()=>dialog?.close()},[]);return <dialog ref={ref} className="modal" onCancel={e=>{e.preventDefault();onClose()}} onClick={e=>{if(e.target===ref.current)onClose()}}><div className="modal-head"><h2>{title}</h2><button className="icon-button" aria-label="Close dialog" onClick={onClose}><X/></button></div>{children}</dialog>}
export function Avatar({name='knight',size='normal'}:{name?:string;size?:string}){return <span className={'avatar '+size+' avatar-'+name} aria-label={name+' avatar'}>{name==='knight'?<svg viewBox="0 0 48 48"><path d="m13 35 6-12-6 2-3-5 10-11 1-6 7 6c11 3 11 17 7 26H13Z"/><path d="M12 39h25M21 16h1" fill="none" stroke="currentColor" strokeWidth="3" strokeLinecap="round"/></svg>:name==='spark'?<Zap/>:name==='nova'?<Sparkles/>:name==='orbit'?<Orbit/>:<Flame/>}</span>}
export const avatars=['knight','spark','nova','orbit','flame'];
export const ranks=[{name:'Spark',xp:0},{name:'Electron',xp:150},{name:'Atom',xp:400},{name:'Supernova',xp:900}];
export function rankFor(xp:number){return [...ranks].reverse().find(r=>xp>=r.xp)||ranks[0]}
export const facts=[{title:'A knight’s little superpower',text:'Knights can jump over other pieces. Their L-shaped move changes square color every time.'},{title:'Five zeros, one NIM',text:'One NIM is divided into 100,000 Luna. A little appreciation can go a long way.'},{title:'The royal shortcut',text:'Castling moves your king and rook together. Neither may have moved, and the king cannot castle through check.'},{title:'Your wallet, your call',text:'Nimiq Pay asks you to approve a tip. Playing a match never automatically sends NIM.'},{title:'Quiet moves count',text:'A move doesn’t need to capture a piece to be strong. Improving your worst-placed piece can change the game.'},{title:'A surprising draw',text:'When a player has no legal move and their king is not in check, the game ends in stalemate.'}];
